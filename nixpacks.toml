# Force Node.js provider (ignore Deno in supabase folder)
providers = ["node"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -s -l ${PORT:-3000}"
